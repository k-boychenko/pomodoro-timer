export enum TimerModes {
    pause = "pause-mode",
    play = "play-mode"
};

export enum PomoModes {
    pomo = "pomodoro-mode",
    shortBreak = "short-break-mode",
    longBreak = "long-break-mode"
};

export enum TimerTitles {
    pomodoro = "Pomodoro",
    shortBreak = "Short Break",
    longBreak = "Long Break"
}

export enum TimerButtonNames {
    start = "START",
    pause = "PAUSE",
    done = "DONE",
    skip ="SKIP"
}

export enum ToastMessages {
    pomodoro = "Break is over. Time to focus!",
    shortBreak = "Time to take a short break!",
    longBreak = "Time to take a long break!"
}

export const enum PlayStatus {
    playing = "PLAYING",
    stopped = "STOPPED",
    paused = "PAUSED"
};

export const enum LogInFormTypes {
    logIn = "logIn",
    signUp = "signUp"
}